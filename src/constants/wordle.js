import colors, { green, white, yellow } from "tailwindcss/colors"

export const ROWS = 6
export const COLS = 5

export const COLORS = {
   white: colors.white,
   black: colors.black,
   green: colors.lime[600],
   yellow: colors.yellow[500],
   grey: colors.gray[500],
   lightGrey: colors.gray[300],
}
